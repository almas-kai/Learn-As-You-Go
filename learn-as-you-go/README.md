# learn-as-you-go

The project is used to create a front-end side of the webapp. To learn more about web-development and Angular.

## Frequently Used Commands

To install necessary packages run `npm install`.

To run the app use `ng serve`.

To run tests use `ng test`.

To generate some components/services/pipes and so on use `ng generate`.
